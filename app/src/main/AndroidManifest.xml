<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="com.songify.app.SongifyApplication"
        android:allowBackup="true"
        android:appComponentFactory="com.songify.app.SongifyAppComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeSample"
        tools:replace="android:appComponentFactory">
        <activity
            android:name="com.songify.app.EntryPointActivity"
            android:exported="true"
            android:theme="@style/Theme.ComposeSample.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
